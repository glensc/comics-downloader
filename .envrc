layout node

# Use Node LTS
export PATH="/usr/local/opt/node@14/bin:$PATH"

# Ensure semver gem takes precedence over npm semver
export PATH="/usr/local/opt/gems/bin:$PATH"

[[ -f .envrc.local ]] && source_env .envrc.local

# vim:ft=bash
